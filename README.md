# AWS-CF-Template-Library
 
 This is a repo for different templates to build infrastructure as code in AWS.
 All templates are written in YAML.
 Following resource templates included
  - aws vpc stack
  - aws transit gateway
  - Two interface Cisco CSR

